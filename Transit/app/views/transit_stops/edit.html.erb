<%= content_for :title, @transit_stop.name %>

<h1>Editing Transit Stop</h1>

<%= render 'form', transit_stop: @transit_stop %>

<%= link_to 'Show', @transit_stop %> |
<%= link_to 'Back', transit_stops_path %>
